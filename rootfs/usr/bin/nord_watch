#!/bin/bash

while true; do
  sleep "${CHECK_CONNECTION_INTERVAL:-300}"
  LOAD=75
  nordvpn_current_server=$(nordvpn status | grep server | cut -b 17-)
  server_load=$(curl -s https://nordvpn.com/api/server/stats/$nordvpn_hostname | jq -r '.[]')

  #Check serverload value is not empty
  if [ -z "$server_load" ];then
    echo "$(adddate) ERROR: No response from NordVPN API to get server load. This check to restart OpenVPN will be ignored."
    continue
  fi

  #Check serverload with expected load
  if [ $server_load -gt $LOAD ]; then
    echo "WARNING: Load on $nordvpn_hostname is to high! Current load is $server_load and expected is $LOAD"
    echo "WARNING: OpenVPN will be restarted!"
    nordvpn connect
  else
    echo "$(adddate) INFO: The current load of $server_load on $nordvpn_hostname is okay"
  fi

done

exit 0
